# Round 3 findings so far (blocked: client signed out)

State at start (2026-09-24 19:44 PDT): the container had restarted about 32 minutes
earlier. WeChat (pid 867) was running on its 280x380 login window, showing the cached
account "zf" with "Enter Weixin". The store keys were still valid, but the client was
signed out. The round-2 accessibility bus had gone with the restart.

Done without touching the account:
- Restarted the round-2 stand-in (`a11y_bus.py`). The running login-window client read
  IsEnabled, called GetAddress and joined the bus within the same second. No restart.
- #505 (`78dde51`) `check` on the login window: `{"ready": false, "reason": "the client
  shows no chat list: signed out, locked, or loading"}`. But `main_frame()` accepts the
  login frame, because the login tool bar holds a hidden `Back` push button and
  `has_chat_list(showing=False)` counts it. A minimized login window would pass `check`.
- The main frame (here the login frame) has the Action interface with an empty action
  list, so AT-SPI offers no action that could raise it.
- `keys_follow_focus_experiment.py` on a scratch display (`:151`, own bus and registry,
  two xterms recording their input): the registry's
  `DeviceEventController.GenerateKeyboardEvent` takes no target. Its keys land in
  whichever window holds X focus, like xdotool: `aa\n` reached the first xterm while it
  had focus, `bb\n` the second.

Not run (needs the signed-in main window): the tree survey, keyboard navigation of the
results, the lag measurement, open without clicking, the send, and the reliability runs.
