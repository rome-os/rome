name: test-all-actions
description: Test agent with wildcard access to public actions.
tier: medium
reasoningEffort: low
permissionMode: bypassPermissions
tools:
  - Read
actions:
  - "*"
systemPromptPrefix: |
  You are a test agent with access to public callable actions.
