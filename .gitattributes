packages/desktop/lima/bin/limactl filter=lfs diff=lfs merge=lfs -text
packages/desktop/lima/share/lima/lima-guestagent.Linux-aarch64.gz filter=lfs diff=lfs merge=lfs -text
packages/desktop/lima/images/*.qcow2 filter=lfs diff=lfs merge=lfs -text

# All video assets go through Git LFS, wherever they live in the repo.
*.mp4 filter=lfs diff=lfs merge=lfs -text

# Drizzle rewrites the whole schema snapshot on every `generate`, so a one-column
# migration lands thousands of diff lines. linguist-generated collapses them in
# GitHub diffs. The .sql migrations carry the reviewable change and stay visible.
**/meta/*_snapshot.json linguist-generated

packages/web/mock/public/recorded-apps/** linguist-vendored
